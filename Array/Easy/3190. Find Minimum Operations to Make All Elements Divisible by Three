/**
 * @param {number[]} nums
 * @return {number}
 */
var minimumOperations = function (nums) {
    let countOperations = 0;
    for (const num of nums) {
        if (num % 3) countOperations++;
    }
    return countOperations;
};